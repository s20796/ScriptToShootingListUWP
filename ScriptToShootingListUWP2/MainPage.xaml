<Page
    x:Class="ScriptToShootingListUWP2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScriptToShootingListUWP2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBox x:Name="myTextBox" HorizontalAlignment="Stretch" VerticalAlignment="Top"
                 Margin="20" TextWrapping="Wrap" AcceptsReturn="True"
                 PlaceholderText="Enter text script..." />

        <Button x:Name="myButton" Content="Send to ChatGPT" HorizontalAlignment="Center"
                VerticalAlignment="Center" Margin="20"
                Click="OnButtonClick" />

        <TextBox x:Name="myOutputTextBox" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                 Margin="20" TextWrapping="Wrap" AcceptsReturn="True"
                 PlaceholderText="Output Text..." />
    </Grid>
</Page>
